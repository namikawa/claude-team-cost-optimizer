# 変更履歴

## [未リリース]

### 追加

- 入出力ディレクトリの既定を `config.yaml` の `paths.input` / `paths.output` で指定できる。
  CSV とレポートをワークスペースの外に置く場合に、コマンドごとの `--input-dir` /
  `--output-dir` が不要になる。相対パスは `config.yaml` の置き場所が基準で、フラグを
  付けた実行ではそちらが優先される

### 変更

- dashboard の行の折りたたみ（「残り N 名を表示」）を廃止し、一覧は最初から全行を表示する。
  表はスクロール領域とその直下のバーで従来どおり高さを調整できる

## [1.1.0] - 2026-08-18

### 追加

- 詳細資料 `details.md` を正式分析で常に生成する。全ユーザ表・部署別/チーム別サマリ・
  詳細利用状況・組織内の分布・月中の推移・込み枠の実測・感度分析を集めた機械生成の文書で、
  `report.md` はサマリ・シート変更推奨・考察を読むための短い文書になった
- `usage-summary.csv` を正式分析で常に生成する。ユーザ単位の product 利用特徴量
  （Code 需要・利用の広がり等）を二次加工できる形で出す
- product の分類設定 `config.yaml > product_policy`。活用の評価を主軸の product に絞って
  見るための設定で、シート判定に使う需要（全 product の合計）は変わらない
- dashboard に「Codeと他プロダクトの需要（API換算）」と「組織内の分布（参考値）」を追加

### 変更

- dashboard を刷新した。4タブ構成（概要 / 推奨アクション / メンバー別 / 組織）、
  テーマ切替（Light / Dark / Auto）、列ソート・検索・判定フィルタ・行の折りたたみ・
  スクロール領域の高さ変更に対応する。JavaScript が無効な環境でも全内容を読める
- メンバー一覧の採用規則を「対象月末に最も近いスナップショット」に変えた。月末までのデータを
  翌月の最初の営業日に取得する運用に合わせたもの。過去の月を再実行すると採用ファイルが変わり、
  判定が変わることがある
- `doctor` は、対象月末の直後（7日以内）のメンバー一覧を採用した場合に警告を出さなくなった

### 修正

- メンバー一覧が対象月に無い場合、対象月から最も遠いスナップショットが選ばれていた。
  新しいファイルが増えるたびに過去月の判定が変わる状態だった

## [1.0.0] - 2026-08-11

初回リリース。

### 追加

- シートコストの損益分岐分析（`analyze`）。claude.ai のスペンドレポート CSV からユーザ×月の
  API 換算需要を集計し、「Standard + 従量課金」と「Premium」のコストを比較してシート変更を
  推奨する。込み利用量（allowance）は非公開のため low / mid / high の 3 シナリオで感度分析し、
  推奨はデータの存在する直近 2 ヶ月での一致と削減額の下限（ヒステリシス）を満たしたものだけを
  「変更推奨」にする
- 出力は組織ごとに `reports/<組織名>/YYYY-MM/` へ 3 種類。`report.md`（前月からの変化・推奨
  テーブル・部署別/チーム別サマリ・詳細利用状況・感度分析・考察）、`dashboard.html`（共有用の
  自己完結 HTML）、`recommendations.csv`（二次加工用）
- 速報モード（`analyze --preview`）。月の途中までのスペンドレポートを月末ペースに日割り換算し、
  一次判断のラベルだけを付ける。正式レポートとヒステリシスには影響しない
- 入力データの事前検査（`doctor`）。対象月の欠損・部分月・ヒステリシス窓の欠月・単価表に無い
  モデル・メンバー一覧との突き合わせ不整合などを検出する。機械可読な JSON 出力に対応
- 考察の自動執筆（`discuss` / `analyze --with-discussion`）。ローカルの Claude Code CLI を
  ヘッドレスで呼び出し、report.md の「## 考察」を書かせる。記入済みの考察は上書きしない
- 組織別レポートに他組織の情報が混ざらないことの機械的な検査。対象組織の資料に現れない
  組織名・メールアドレス・人名・部署名を検出したら書き直しを求め、残る場合は書き込まずに
  終了コード 1 を返す
- 公開テキストの検査（`check-text`）。PR 本文・コメント・コミットメッセージなど、リポジトリの
  外に出る文章に業務情報が含まれないかを、レポートと同じ照合規則で確かめる。差分を対象に
  する `--diff` に対応
- マルチ組織。`input/<組織名>/` ごとに独立して分析し、複数組織を一括実行した場合は
  `reports/summary/YYYY-MM.md` に組織横断サマリを出力する
- 任意の `members-info.csv`（部署・チーム・職種・追加クレジット上限・備考）。置くとレポートに
  部署別・チーム別のサマリ、追加クレジットの構成と上限到達の警告、クレジット付与候補が加わる
- 前月からの変化のセクション。利用開始・利用停止・主な増減・実課金の新規発生・月次推移を
  表示する（判定には影響しない）
- スナップショット差分。同じ月に月初からの累積スペンドを複数回エクスポートしておくと、月中の
  利用の伸び・停止と込み量の消化を検出する。members と code-analytics のスナップショットにも
  対応（月中のシート変更・メンバー変動・LoC の伸び）
- ワークスペースとプログラムの分離。`uv tool install` で導入し、`init` が任意の場所に
  ワークスペース（`input/`・`config.yaml`・`.gitignore`）を、`init-org` が組織ディレクトリの
  雛形を作る。設定はパッケージ同梱の既定に対して差分だけを書く上書き方式で、単価やカラム
  対応表の更新はアップデートで届く。`--version` でバージョンを確認できる
- macOS / Windows / Linux で動作確認済み（CI で 3 OS すべてのテストを実行）

[1.0.0]: https://github.com/namikawa/claude-team-cost-optimizer/releases/tag/v1.0.0
