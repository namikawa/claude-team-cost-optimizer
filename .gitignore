# 実データ（個人情報・利用実績を含むためコミット禁止）
# ルート直下のみ。examples/input/ の合成サンプルは対象外
/input/
reports/

# ワークスペースの上書き設定（組織固有の設定＝実在の部署名等が入りうるためコミット禁止。
# 既定は src/seat_analyzer/default-config.yaml が持つ）
/config.yaml

CLAUDE.md
.claude/settings.local.json
.claude/agents/
.DS_Store
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
